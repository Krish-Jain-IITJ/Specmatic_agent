{
  "_comment": "RESILIENCY TEST: GET /memory with NO session_id query param. The spec says session_id defaults to 'default'. This tests that the server does NOT throw a KeyError on conversation_memory['default'] when 'default' has never been written — it should return an empty sessions list, not a 500.",
  "http-request": {
    "path": "/memory",
    "method": "GET",
    "headers": {
      "Accept": "application/json"
    }
  },
  "http-response": {
    "status": 200,
    "status-text": "OK",
    "headers": {
      "Content-Type": "application/json"
    },
    "body": {
      "sessions": []
    }
  }
}
